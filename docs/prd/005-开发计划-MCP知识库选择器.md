# MCP 知识库选择器 — 完整开发计划

> 基于 CLI 源码（`packages/opencode/src/mcp/`）深度验证后的最终方案。
> 结论：`GET /mcp` 端点不返回工具信息，必须通过 CLI 插件 `tool.definition` Hook 捕获。

---

## 一、已验证的源码事实

### 1.1 CLI `/mcp` 端点返回格式

```
packages/opencode/src/mcp/index.ts:616-633

MCP.status() → Record<string, Status>

Status = Schema.Union([
    StatusConnected,        // { status: "connected" }
    StatusDisabled,         // { status: "disabled" }
    StatusFailed,           // { status: "failed", error: string }
    StatusNeedsAuth,        // { status: "needs_auth" }
    StatusNeedsClientRegistration,
])
```

**不包含工具信息。** 工具数据存储在 CLI 内部 `s.defs[name]`，不通过 HTTP 暴露。

### 1.2 工具发现机制

```
packages/opencode/src/mcp/catalog.ts:38-40, 145-162

defs(client, timeout)
  → listTools(client, timeout)
    → client.listTools(params, { timeout })    ← MCP 协议标准方法
    → paginate()                               ← 分页获取全部工具

catalog.ts:119
toolName(clientName, name) = sanitize(clientName) + "_" + sanitize(name)
// 示例: "knowledge-base" + "search" → "knowledge-base_search"
```

### 1.3 旧版 vs 新版对比

| 维度 | 旧版 (Roo Code VS Code) | 新版 (Kilo JetBrains) |
|---|---|---|
| MCP 管理 | 扩展宿主直接管理 `McpHub.ts` | CLI 远程管理 |
| 工具获取 | `client.request({ method: "tools/list" })` | CLI 内部调用，不外暴 |
| 工具存储 | `connection.server.tools` 内存对象 | `s.defs[name]` CLI 内部状态 |
| 插件可读 | ✅ IPC 全量读取 | ❌ RPC 只返回 name + status |

---

## 二、架构设计

### 2.1 三层协作模型

```
┌─ CLI 插件 (.kilo/plugin/mcp-selector.ts) ──────────┐
│                                                      │
│  tool.definition Hook                                │
│  ↓ 拦截全部工具（内置 + MCP）                         │
│  ↓ 过滤出 MCP 工具                                   │
│  ↓ 按 server 分组                                    │
│  写入 → {project}/.kilo/.cache/mcp-tools.json        │
│                                                      │
│  system.transform Hook                               │
│  ↓ 读取 {project}/.kilo/.cache/active-mcp.json       │
│  ↓ 注入 "请优先使用以下工具: xxx"                     │
│                                                      │
└──────────────────┬───────────────────────────────────┘
                   │ 文件系统（项目级）
┌──────────────────▼───────────────────────────────────┐
│ JetBrains 插件 (custom/)                              │
│                                                      │
│  PromptPanel 工具栏 MCP 按钮 (1 行 custom_change)     │
│  ↓ 点击                                              │
│  读取 .kilo/.cache/mcp-tools.json                     │
│  ↓                                                   │
│  弹窗：树形展示服务器 + 工具 + 勾选                    │
│  ↓ 用户确认                                          │
│  写入 .kilo/.cache/active-mcp.json                    │
│                                                      │
└──────────────────────────────────────────────────────┘
```

### 2.2 数据流时序

```
T0: CLI 启动
T1: MCP 服务器连接（catalog.ts 发现工具）
T2: tool.definition Hook 触发（首次或每次工具列表变更）
T3: CLI 插件写入 mcp-tools.json
    ──────────────────────── 用户可操作 ────────────────────────
T4: 用户点击 PromptPanel MCP 按钮
T5: JetBrains 读取 mcp-tools.json → 弹窗展示
T6: 用户勾选工具 → 确认
T7: JetBrains 写入 active-mcp.json
    ──────────────────────── 每次对话 ────────────────────────
T8: 用户发送消息
T9: system.transform Hook 触发
T10: CLI 插件读取 active-mcp.json
T11: 注入 "请优先使用工具: xxx" 到 system prompt
T12: LLM 收到引导 → 优先调用选中的 MCP 工具
```

---

## 三、数据格式定义

### 3.1 mcp-tools.json（CLI 插件写入）

```json
{
  "updatedAt": "2026-08-13T12:00:00.000Z",
  "servers": [
    {
      "name": "knowledge-base",
      "tools": [
        {
          "name": "knowledge-base_search",
          "description": "搜索项目知识库中的相关内容"
        },
        {
          "name": "knowledge-base_query",
          "description": "查询知识库文档"
        }
      ]
    },
    {
      "name": "filesystem",
      "tools": [
        {
          "name": "filesystem_read_file",
          "description": "读取文件内容"
        },
        {
          "name": "filesystem_write_file",
          "description": "写入文件内容"
        }
      ]
    }
  ]
}
```

### 3.2 active-mcp.json（JetBrains 写入）

```json
{
  "updatedAt": "Wed Aug 13 12:30:00 CST 2026",
  "selectedTools": [
    "knowledge-base_search",
    "knowledge-base_query"
  ],
  "instruction": "请优先使用知识库工具检索相关信息后再回答"
}
```

### 3.3 文件不存在时的行为

| 文件 | 不存在时 | 说明 |
|---|---|---|
| mcp-tools.json | 按钮提示"未检测到 MCP 工具" | CLI 未连接任何 MCP 服务器 |
| active-mcp.json | 不注入引导指令 | AI 正常对话，不特别引导 MCP |

---

## 四、CLI 插件实现（mcp-selector.ts）

### 4.1 完整代码

```typescript
/**
 * MCP 知识库选择器 CLI 插件
 *
 * 功能：
 * - tool.definition: 捕获所有 MCP 工具，写入缓存
 * - system.transform: 读取用户选择，注入优先使用引导
 *
 * 放置位置: .kilo/plugin/mcp-selector.ts
 */

import type { Plugin } from "@kilocode/plugin"
import { writeFileSync, readFileSync, existsSync, mkdirSync } from "node:fs"
import { join } from "node:path"

const BUILTIN_TOOLS = new Set([
  "read", "write", "edit", "bash", "grep", "glob",
  "webfetch", "websearch", "question", "task",
  "todowrite", "todoread", "plan", "skill",
  "agent_manager", "notify_user",
  "kilo-playwright_navigate", "kilo-playwright_click",
  "kilo-playwright_fill", "kilo-playwright_screenshot",
  "kilo-playwright_evaluate", "kilo-playwright_close",
  // 内置工具前缀（动态生成的 playwright 工具）
])

const BUILTIN_PREFIXES = ["kilo-playwright_", "playwright_"]

function isBuiltin(name: string): boolean {
  if (BUILTIN_TOOLS.has(name)) return true
  return BUILTIN_PREFIXES.some(prefix => name.startsWith(prefix))
}

function ensureDir(dir: string) {
  if (!existsSync(dir)) mkdirSync(dir, { recursive: true })
}

function writeJson(path: string, data: unknown) {
  ensureDir(join(path, ".."))
  writeFileSync(path, JSON.stringify(data, null, 2), "utf-8")
}

function readJson(path: string): any | null {
  try {
    return JSON.parse(readFileSync(path, "utf-8"))
  } catch {
    return null
  }
}

const McpSelectorPlugin: Plugin = async ({ directory }) => {
  const cacheDir = join(directory, ".kilo", ".cache")
  const toolsPath = join(cacheDir, "mcp-tools.json")
  const activePath = join(cacheDir, "active-mcp.json")

  return {
    // ---- 捕获所有工具定义，写入缓存 ----
    "tool.definition": async (tools) => {
      try {
        const mcpTools = tools.filter(t => !isBuiltin(t.name))

        const serverMap = new Map<string, Array<{ name: string; description: string }>>()
        for (const tool of mcpTools) {
          // MCP 工具命名: {server}_{tool}
          const idx = tool.name.indexOf("_")
          const serverName = idx > 0 ? tool.name.substring(0, idx) : tool.name
          if (!serverMap.has(serverName)) {
            serverMap.set(serverName, [])
          }
          serverMap.get(serverName)!.push({
            name: tool.name,
            description: tool.description || "",
          })
        }

        const data = {
          updatedAt: new Date().toISOString(),
          servers: Array.from(serverMap.entries()).map(([name, tools]) => ({
            name,
            tools,
          })),
        }
        writeJson(toolsPath, data)
      } catch {
        // 静默失败
      }

      return tools
    },

    // ---- 每次对话前注入 MCP 选择引导 ----
    "experimental.chat.system.transform": async (_input, output) => {
      try {
        const active = readJson(activePath)
        if (active && active.selectedTools && active.selectedTools.length > 0) {
          const toolList = active.selectedTools.join(", ")
          const instruction = active.instruction || "请优先使用以下工具"
          output.system.push(
            `## MCP 工具优先使用规则\n${instruction}：${toolList}`
          )
        }
      } catch {
        // 静默失败
      }
    },
  }
}

export default { id: "mcp-selector", server: McpSelectorPlugin }
```

### 4.2 验证逻辑

| 测试项 | 方法 | 预期 |
|---|---|---|
| 工具捕获 | 配置 MCP → 重启会话 → 检查 .kilo/.cache/mcp-tools.json | JSON 包含 server + tools |
| 内置工具过滤 | 检查 mcp-tools.json | 不含 read/write/bash 等 |
| 引导注入 | 选择工具 → 发送消息 → 检查 CLI 日志 | system prompt 含工具引导 |
| 无选择时不注入 | 清空 active-mcp.json → 发送消息 | system prompt 无 MCP 引导 |
| 文件不存在 | 删除 mcp-tools.json → 按钮 | 提示"未检测到" |

---

## 五、JetBrains 插件实现

### 5.1 新增文件清单

| 文件 | 行数 | 说明 |
|---|---|---|
| `custom/prompt/McpToolCache.kt` | ~80 | 读写缓存文件 |
| `custom/prompt/McpSelectorButton.kt` | ~60 | 工具栏按钮 |
| `custom/prompt/McpSelectorDialog.kt` | ~150 | 树形选择弹窗 |

### 5.2 McpToolCache.kt

```kotlin
package com.dmc.prompt

import com.google.gson.Gson
import com.google.gson.annotations.SerializedName
import com.intellij.openapi.project.Project
import java.io.File

data class McpToolInfo(
    val name: String,
    val description: String = "",
)

data class McpServerInfo(
    val name: String,
    val tools: List<McpToolInfo> = emptyList(),
)

data class McpToolsCache(
    val servers: List<McpServerInfo> = emptyList(),
)

data class ActiveMcpSelection(
    @SerializedName("selectedTools") val selectedTools: List<String> = emptyList(),
    @SerializedName("instruction") val instruction: String = "",
)

object McpToolCache {

    private val gson = Gson()

    private fun cacheDir(project: Project): File =
        File("${project.basePath}/.kilo/.cache")

    fun readTools(project: Project): List<McpServerInfo> {
        val file = File(cacheDir(project), "mcp-tools.json")
        if (!file.exists()) return emptyList()
        return try {
            // 直接解析 JSON，取 servers 数组
            val root = com.google.gson.JsonParser.parseString(file.readText()).asJsonObject
            val serversArray = root.getAsJsonArray("servers") ?: return emptyList()
            serversArray.map { serverElem ->
                val serverObj = serverElem.asJsonObject
                McpServerInfo(
                    name = serverObj.get("name")?.asString ?: "",
                    tools = serverObj.getAsJsonArray("tools")?.map { toolElem ->
                        val toolObj = toolElem.asJsonObject
                        McpToolInfo(
                            name = toolObj.get("name")?.asString ?: "",
                            description = toolObj.get("description")?.asString ?: "",
                        )
                    } ?: emptyList(),
                )
            }.filter { it.name.isNotEmpty() }
        } catch (e: Exception) {
            emptyList()
        }
    }

    fun readActive(project: Project): ActiveMcpSelection? {
        val file = File(cacheDir(project), "active-mcp.json")
        if (!file.exists()) return null
        return try {
            gson.fromJson(file.readText(), ActiveMcpSelection::class.java)
        } catch (e: Exception) {
            null
        }
    }

    fun writeActive(project: Project, selectedTools: List<String>, instruction: String) {
        val dir = cacheDir(project)
        if (!dir.exists()) dir.mkdirs()
        val data = mapOf(
            "updatedAt" to java.util.Date().toString(),
            "selectedTools" to selectedTools,
            "instruction" to instruction,
        )
        File(dir, "active-mcp.json").writeText(gson.toJson(data), Charsets.UTF_8)
    }

    fun clearActive(project: Project) {
        File(cacheDir(project), "active-mcp.json").delete()
    }
}
```

### 5.3 McpSelectorButton.kt

```kotlin
package com.dmc.prompt

import com.intellij.icons.AllIcons
import com.intellij.notification.Notification
import com.intellij.notification.NotificationType
import com.intellij.openapi.project.Project
import com.intellij.openapi.ui.popup.JBPopup
import com.intellij.openapi.ui.popup.JBPopupFactory
import com.intellij.ui.awt.RelativePoint
import com.intellij.util.ui.JBUI
import javax.swing.JButton

class McpSelectorButton(private val project: Project) : JButton() {

    init {
        icon = AllIcons.Nodes.DataTables
        toolTipText = "MCP 知识库选择"
        isFocusPainted = false
        isContentAreaFilled = false
        isBorderPainted = false
        border = JBUI.Borders.empty(2)
        addActionListener { showPopup() }
    }

    private fun showPopup() {
        val servers = McpToolCache.readTools(project)

        if (servers.isEmpty()) {
            Notification(
                "Kilo Code",
                "未检测到 MCP 工具。请在 kilo.jsonc 中配置 MCP 服务器并重启会话。",
                NotificationType.WARNING,
            ).notify(project)
            return
        }

        val active = McpToolCache.readActive(project)
        val selectedSet = active?.selectedTools?.toSet() ?: emptySet()

        val panel = McpSelectorDialog(servers, selectedSet) { selectedTools, instruction ->
            if (selectedTools.isEmpty()) {
                McpToolCache.clearActive(project)
            } else {
                McpToolCache.writeActive(project, selectedTools, instruction)
            }
        }

        val popup: JBPopup = JBPopupFactory.getInstance()
            .createComponentPopupBuilder(panel, null)
            .setTitle("MCP 知识库选择")
            .setResizable(true)
            .setMovable(false)
            .setFocusable(true)
            .createPopup()

        popup.show(RelativePoint.getSouthWestOf(this))
    }
}
```

### 5.4 McpSelectorDialog.kt

```kotlin
package com.dmc.prompt

import com.intellij.icons.AllIcons
import com.intellij.ui.ColoredTreeCellRenderer
import com.intellij.ui.SimpleTextAttributes
import com.intellij.ui.components.JBScrollPane
import com.intellij.ui.treeStructure.Tree
import java.awt.BorderLayout
import java.awt.Dimension
import java.awt.FlowLayout
import java.awt.event.MouseAdapter
import java.awt.event.MouseEvent
import javax.swing.JButton
import javax.swing.JPanel
import javax.swing.JTextArea
import javax.swing.JTree
import javax.swing.tree.DefaultMutableTreeNode
import javax.swing.tree.DefaultTreeModel
import javax.swing.tree.TreeSelectionModel

class McpSelectorDialog(
    servers: List<McpServerInfo>,
    initiallySelected: Set<String>,
    private val onConfirm: (selectedTools: List<String>, instruction: String) -> Unit,
) : JPanel(BorderLayout()) {

    private val toolStates = mutableMapOf<String, Boolean>()
    private val instructionArea = JTextArea(2, 40)
    private var popup: com.intellij.openapi.ui.popup.JBPopup? = null

    init {
        // 构建树
        val root = DefaultMutableTreeNode("MCP 服务器")
        for (server in servers) {
            val serverNode = DefaultMutableTreeNode(server)
            for (tool in server.tools) {
                toolStates[tool.name] = initiallySelected.contains(tool.name)
                serverNode.add(DefaultMutableTreeNode(tool))
            }
            root.add(serverNode)
        }

        val tree = Tree(DefaultTreeModel(root))
        tree.selectionMode = TreeSelectionModel.SINGLE_TREE_SELECTION
        tree.cellRenderer = McpTreeCellRenderer(toolStates)
        tree.isRootVisible = true

        // 点击工具节点切换勾选
        tree.addMouseListener(object : MouseAdapter() {
            override fun mouseClicked(e: MouseEvent) {
                val path = tree.getPathForLocation(e.x, e.y) ?: return
                val node = path.lastPathComponent as? DefaultMutableTreeNode ?: return
                val tool = node.userObject as? McpToolInfo ?: return
                toolStates[tool.name] = !(toolStates[tool.name] ?: false)
                tree.repaint()
            }
        })

        val scrollPane = JBScrollPane(tree)
        scrollPane.preferredSize = Dimension(500, 350)
        add(scrollPane, BorderLayout.CENTER)

        // 底部区域
        val bottomPanel = JPanel(BorderLayout())
        instructionArea.text = "请优先使用以下工具检索相关信息后再回答"
        instructionArea.lineWrap = true
        instructionArea.wrapStyleWord = true
        bottomPanel.add(javax.swing.JLabel("引导指令:"), BorderLayout.NORTH)
        bottomPanel.add(instructionArea, BorderLayout.CENTER)

        val buttonRow = JPanel(FlowLayout(FlowLayout.RIGHT))
        val selectAllBtn = JButton("全选")
        selectAllBtn.addActionListener {
            toolStates.keys.forEach { toolStates[it] = true }
            tree.repaint()
        }
        val clearAllBtn = JButton("清除")
        clearAllBtn.addActionListener {
            toolStates.keys.forEach { toolStates[it] = false }
            tree.repaint()
        }
        val confirmBtn = JButton("确定", AllIcons.Actions.MenuSaveall)
        confirmBtn.addActionListener { confirm() }
        val cancelBtn = JButton("取消", AllIcons.Actions.Cancel)
        cancelBtn.addActionListener { popup?.closeOk(null) }

        buttonRow.add(selectAllBtn)
        buttonRow.add(clearAllBtn)
        buttonRow.add(confirmBtn)
        buttonRow.add(cancelBtn)
        bottomPanel.add(buttonRow, BorderLayout.SOUTH)

        add(bottomPanel, BorderLayout.SOUTH)
    }

    fun setPopup(popup: com.intellij.openapi.ui.popup.JBPopup) {
        this.popup = popup
    }

    private fun confirm() {
        val selected = toolStates.filter { it.value }.keys.toList()
        onConfirm(selected, instructionArea.text.trim())
        popup?.closeOk(null)
    }

    private class McpTreeCellRenderer(
        private val checkBoxes: Map<String, Boolean>,
    ) : ColoredTreeCellRenderer() {
        override fun customizeCellRenderer(
            tree: JTree,
            value: Any,
            selected: Boolean,
            expanded: Boolean,
            leaf: Boolean,
            row: Int,
            hasFocus: Boolean,
        ) {
            val node = value as? DefaultMutableTreeNode ?: return
            when (val obj = node.userObject) {
                is McpServerInfo -> {
                    icon = AllIcons.Nodes.Folder
                    append(obj.name, SimpleTextAttributes.REGULAR_BOLD_ATTRIBUTES)
                    append("  (${obj.tools.size} 工具)", SimpleTextAttributes.GRAYED_ATTRIBUTES)
                }
                is McpToolInfo -> {
                    val isChecked = checkBoxes[obj.name] ?: false
                    icon = if (isChecked) AllIcons.Actions.Checked else AllIcons.Actions.Unchecked
                    append(obj.name, SimpleTextAttributes.REGULAR_ATTRIBUTES)
                    if (obj.description.isNotEmpty()) {
                        append("  — ${obj.description}", SimpleTextAttributes.GRAYED_SMALL_ATTRIBUTES)
                    }
                }
                is String -> {
                    icon = AllIcons.Nodes.DataTables
                    append(obj, SimpleTextAttributes.REGULAR_ATTRIBUTES)
                }
            }
        }
    }
}
```

### 5.5 PromptPanel.kt 上游改动（1 行）

位置：`frontend/src/main/kotlin/ai/kilocode/client/session/ui/prompt/PromptPanel.kt`

在 `init` 块中 `bar.add(enhance)` 之后（约 line 298）：

```kotlin
        bar.add(enhance)
        // custom_change
        bar.add(com.dmc.prompt.McpSelectorButton(project))
        // /custom_change
        bar.add(Box.createHorizontalStrut(JBUI.scale(SessionUiStyle.View.Prompt.CONTROL_GAP)))
```

---

## 六、上游改动总表

| 文件 | 改动 | 标记 |
|---|---|---|
| `PromptPanel.kt` ~line 298 | `bar.add(McpSelectorButton(project))` | `// custom_change` |

**仅 1 处上游改动，1 行代码。**

---

## 七、实现步骤与验证

| 步骤 | 内容 | 验证 | 预计 |
|---|---|---|---|
| **1** | CLI 插件 `mcp-selector.ts` | 配置 MCP → 检查 mcp-tools.json 生成 | 1h |
| **2** | McpToolCache.kt | 手动创建 mcp-tools.json → 单元测试读写 | 0.5h |
| **3** | McpSelectorButton.kt | IDE 工具栏出现按钮 → 点击弹窗 | 0.5h |
| **4** | McpSelectorDialog.kt | 树形展示 + 勾选 + 确定 | 2h |
| **5** | PromptPanel.kt custom_change | 工具栏出现 MCP 按钮 | 0.5h |
| **6** | 端到端联调 | 选择工具 → 对话 → AI 优先调用 | 1h |

**总计：~5.5h**

---

## 八、端到端验证用例

| # | 前置条件 | 操作 | 预期结果 |
|---|---|---|---|
| 1 | 已配置 MCP 服务器 | 重启 Kilo 会话 | `.kilo/.cache/mcp-tools.json` 生成 |
| 2 | mcp-tools.json 存在 | 打开 IDE，检查 PromptPanel | 工具栏出现 MCP 按钮（数据库图标） |
| 3 | MCP 按钮存在 | 点击 | 弹出选择器，列出服务器+工具 |
| 4 | 选择器已打开 | 勾选工具 + 填指令 + 确定 | `active-mcp.json` 写入 |
| 5 | active-mcp.json 存在 | 发送对话 | AI 优先调用选中工具 |
| 6 | 已有选择 | 点击"清除" + 确定 | `active-mcp.json` 删除 |
| 7 | 无 active-mcp.json | 发送对话 | 正常对话，无 MCP 引导 |
| 8 | 未配置 MCP | 点击 MCP 按钮 | 提示"未检测到 MCP 工具" |
| 9 | 多个 MCP 服务器 | 打开选择器 | 所有服务器的工具都列出 |
| 10 | 点击服务器节点 | — | 展开显示该服务器所有工具 |
| 11 | 点击"全选" | — | 所有工具勾选 |
| 12 | 切换项目 | 打开 MCP 按钮 | 各项目独立工具列表 |

---

## 九、部署步骤

### 9.1 CLI 插件部署

```bash
# 复制到全局（所有项目生效）
cp .kilo/plugin/mcp-selector.ts ~/.config/kilo/plugin/

# 或复制到指定项目
cp .kilo/plugin/mcp-selector.ts <project>/.kilo/plugin/
```

### 9.2 JetBrains 插件部署

```bash
npm run build
# 产物: packages/kilo-jetbrains/build/distributions/*.zip
# 安装: File → Settings → Plugins → Install from Disk
```

### 9.3 验证流程

```bash
# 1. 配置 MCP（在 kilo.jsonc 中）
# 2. 重启 Kilo 会话
# 3. 检查工具缓存
cat .kilo/.cache/mcp-tools.json

# 4. IDE 中点击 MCP 按钮 → 选择工具
# 5. 检查选择缓存
cat .kilo/.cache/active-mcp.json

# 6. 发送对话 → 验证 AI 行为
```

---

## 十、风险与缓解

| 风险 | 等级 | 缓解 |
|---|---|---|
| `tool.definition` Hook 未触发 | 低 | CLI 每次 LLM 调用前必然触发 |
| mcp-tools.json 写入延迟 | 低 | 异步写入，下次对话一定可用 |
| MCP 服务器名解析不准 | 中 | `{server}_{tool}` 命名规则已从源码验证（catalog.ts:119） |
| 选择器弹窗尺寸 | 低 | setResizable(true)，用户可拖拽 |
| `project` 变量不可见 | 低 | PromptPanel 构造参数，作用域内可见 |
| 并发读写 mcp-tools.json | 低 | CLI 写 + JetBrains 读，单向数据流，无竞争 |
